# Конфигурация 2 — ЛОКАЛ ПРОД (реальный бэк). Прод-сборка + `vite preview` локально.
# Запросы на /api/v1 проксируются на локальный бэк (по умолчанию http://localhost:8080,
# переопределяется переменной окружения BACKEND_ORIGIN, см. vite.config.ts).
VITE_API_MODE=real
VITE_API_BASE_URL=/api/v1

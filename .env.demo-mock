# Конфигурация 3 — ДЕМО ДЕВ (моки на сервере). Прод-сборка с MSW, раздаётся nginx.
# Бэк не нужен; данные живут в браузере посетителя. Требует HTTPS (Service Worker).
VITE_API_MODE=mock
VITE_API_BASE_URL=/api/v1

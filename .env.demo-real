# Конфигурация 4 — ДЕМО ПРОД (реальный бэк на сервере). Прод-сборка без моков.
# nginx раздаёт статику и проксирует /api/v1 на бэк (см. deploy/nginx-stringly.conf).
VITE_API_MODE=real
VITE_API_BASE_URL=/api/v1
